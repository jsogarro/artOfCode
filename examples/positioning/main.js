'use strict';

// Get text from first name input field
var inputFirstNameValue = $('input#first').val();

// Add event listener to button
$('button').on('click', function() {
    // TODO: Write some logic to get the values out of the inputs and do sometthing with it
    console.log('clicked!!!');
});
